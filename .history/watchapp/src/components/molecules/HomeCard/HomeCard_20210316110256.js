import React from 'react';
import PropTypes from 'prop-types';

const HomeCard = ({content, title}) => (
    
)

PropTypes.HomeCard = {
    content: PropTypes.string.isRequired,
    title: PropTypes.string.isRequired,
}

export default HomeCard;